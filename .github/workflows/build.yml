name: Build Plugin

on:
  workflow_dispatch:
  push:
    branches:
      - XLRLightweightChat
  pull_request:
    branches:
      - XLRLightweightChat

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven

    - name: Build with Maven
      run: mvn clean package -DskipTests

    - name: Upload plugin JAR
      uses: actions/upload-artifact@v4
      with:
        name: XLRLightweightChat-${{ github.sha }}
        path: target/*.jar
